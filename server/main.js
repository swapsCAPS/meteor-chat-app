import { Meteor } from 'meteor/meteor';

import '../imports/api/messages.js'
import '../imports/api/chatsList.js'
import '../imports/api/usersList.js'

Meteor.startup(() => {
  // code to run on server at startup
});
